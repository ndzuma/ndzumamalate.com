{
  "name": "ndzumamalate.com",
  "private": true,
  "packageManager": "bun@1.3.10",
  "workspaces": [
    "apps/web",
    "apps/cms"
  ],
  "scripts": {
    "dev": "turbo run dev dev:api --filter=./apps/cms --filter=// --parallel --ui=tui",
    "dev:api": "cd apps/api && go run ./cmd/server",
    "build": "turbo build"
  },
  "devDependencies": {
    "turbo": "^2.8.17"
  }
}
