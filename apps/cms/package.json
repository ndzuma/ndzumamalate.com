{
  "name": "cms",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:api": "rm -rf ../api/internal/cms/dist && mkdir -p ../api/internal/cms/dist && cp -R dist/. ../api/internal/cms/dist/"
  },
  "dependencies": {
    "@fontsource-variable/geist": "^5.2.8",
    "markdown-it": "^14.1.1",
    "phosphor-svelte": "^3.1.0",
    "svelte": "^5.53.12"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "vite": "^8.0.0"
  }
}
